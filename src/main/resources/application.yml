spring:
  application:
    name: langgomsport
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DATABASE_HOST:172.31.26.248}:${DATABASE_PORT:3306}/${DATABASE_NAME:langgomsport}?createDatabaseIfNotExist=true
    username: ${DATABASE_USERNAME:admin}
    password: ${DATABASE_PASSWORD:123456}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: false
    locations: classpath:db/migrations
    baseline-on-migrate: false
    validate-on-migrate: false

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:172.31.26.248}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      repositories:
        enabled: false
