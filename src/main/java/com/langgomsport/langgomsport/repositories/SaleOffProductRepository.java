package com.langgomsport.langgomsport.repositories;

import com.langgomsport.langgomsport.models.SaleOffProduct;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SaleOffProductRepository extends JpaRepository<SaleOffProduct, Integer> {
}
